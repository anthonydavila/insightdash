import React from 'react';
import { Container } from 'react-bootstrap';

const Header = () => (
  <Container className="text-center mt-4 bg-primary text-light py-3">
    <h1>Insight Dash</h1>
  </Container>
);

export default Header;
